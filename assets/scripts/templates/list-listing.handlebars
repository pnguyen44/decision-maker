{{#each lists as |list|}}
  <div class="col-xs-12 col-sm-6 col-md-4 col-lg-3 box">
  {{> add-list-template list = list}}

    {{#each list.items as |item|}}
      {{> add-item-template item = item}}
    {{/each}}
  </div>
{{/each}}
